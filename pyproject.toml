[project]
name = "exo-executive-agent-distribution"
version = "0.1.0"
description = "Hermes profile distribution validation and local smoke tooling for Exo."
requires-python = ">=3.11"
dependencies = []

[tool.uv]
package = false

[tool.ruff]
line-length = 100
target-version = "py311"
